# API Configuration
VITE_API_BASE_URL=https://api.diycomponents.in

# Optional: Encryption secrets (if using encrypted API communication)
# VITE_ENCRYPTION_SECRET=your-encryption-secret
# VITE_SIGNING_SECRET=your-signing-secret
